# AppVeyor CI settings (Windows Machine CI Tests)

environment:
  matrix:
    - TOXENV: py38
    - TOXENV: py37
    - TOXENV: py36
    - TOXENV: py35
    - TOXENV: lint-py36
    - TOXENV: py27

install:
  - pip install tox

build: off

max_jobs: 2

test_script:
  - tox
